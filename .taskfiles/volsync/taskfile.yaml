---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

output: group

tasks:
  stats:
    desc: Show VolSync/Kopia repository statistics [CLUSTER=main]
    cmds:
      - bash {{.ROOT_DIR}}/.taskfiles/volsync/stats.sh
    requires:
      vars: [CLUSTER]
    preconditions:
      - which kubectl jq bc
