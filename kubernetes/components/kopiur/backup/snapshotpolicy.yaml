---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kopiur.home-operations.com/snapshotpolicy_v1alpha1.json
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotPolicy
metadata:
  name: ${APP}
spec:
  repository:
    kind: ClusterRepository
    name: kopia-nas
  sources:
    - pvc:
        name: ${APP}
      # The fork's mover always recorded /data regardless of the app's real
      # mount path — kopiur's own default (/pvc/<name>) would start a new
      # identity strand instead of continuing the adopted history.
      sourcePathOverride: /data
  mover:
    inheritSecurityContextFrom:
      pvcConsumer: {}
  compression:
    compressor: zstd
  # keepHourly lowered from 24: kopiur's bootstrap-result ConfigMap hits a
  # hardcoded 1MiB cap around ~515-519 total snapshots across the shared
  # repo (home-operations/kopiur#237, unfixed upstream). 24+7=31/app × 17
  # apps ≈ 527, right at the ceiling; 8+7=15/app × 17 ≈ 255 buys headroom
  # until it's fixed.
  retention:
    keepHourly: 8
    keepDaily: 7
  volumeSnapshotClassName: ${BACKUP_SNAPSHOTCLASS:=csi-ceph-blockpool}
  credentialProjection:
    enabled: true
