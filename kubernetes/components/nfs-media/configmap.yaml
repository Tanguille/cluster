---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nfs-media
data:
  values.yaml: |
    persistence:
      media:
        type: nfs
        server: ${TRUENAS_IP}
        path: ${MEDIA_NFS_PATH}
        globalMounts:
          - path: /media
            readOnly: false
