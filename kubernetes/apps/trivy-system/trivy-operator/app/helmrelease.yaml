---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app trivy-operator
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: trivy-operator
  values:
    fullnameOverride: *app
    # Fully on-prem: Standalone mode (in-cluster scanner jobs), no ARMO/external
    # backend. rbac/serviceAccount are created by the chart (both default true).
    operator:
      replicas: 1
    image:
      # aquasec publishes the operator image on Docker Hub, not ghcr.io
      # (ghcr.io/aquasec/trivy-operator is 403), so pin the registry explicitly.
      registry: docker.io
      repository: aquasec/trivy-operator
    trivy:
      image:
        # aquasec/trivy scanner image is on Docker Hub, not ghcr.io (403 there).
        registry: docker.io
        repository: aquasec/trivy
      # Slow mode: gentler on CPU/RAM at the cost of slower scans — right size for a homelab.
      slow: true
      severity: UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL
    # Expose the /metrics endpoint to Prometheus via a ServiceMonitor.
    serviceMonitor:
      enabled: true
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 512Mi
