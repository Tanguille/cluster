---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openebs-system
components:
  - ../../components/common
  # localpv spawns a privileged hostPath helper pod per PVC create and delete; baseline rejects it.
  - ../../components/psa-privileged
resources:
  - ./openebs/ks.yaml
