---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: obico
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
  hostnames:
    - "obico.${SECRET_DOMAIN}"
  rules:
    - backendRefs:
        - name: obico
          port: 3334
