---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: moonraker-obico
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template

  values:
    defaultPodOptions:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault

    controllers:
      moonraker-obico:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          render-config:
            image:
              repository: busybox
              tag: 1.38.0@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
            command:
              - /bin/sh
              - -ec
              - |
                config_file=/opt/printer_data/config/moonraker-obico.cfg
                token="`awk '
                  /^\[server\]/ { in_server=1; next }
                  /^\[/ { in_server=0 }
                  in_server {
                    line=$0
                    sub(/^[[:space:]]*/, "", line)
                    if (line ~ /^auth_token[[:space:]]*=/) {
                      sub(/^auth_token[[:space:]]*=[[:space:]]*/, "", line)
                      print line
                      exit
                    }
                  }
                ' "$config_file" 2>/dev/null || true`"

                awk -v token="$token" '
                  { print }
                  /^\[server\]/ && token != "" && !done {
                    print "auth_token = " token
                    done=1
                  }
                ' /config-template/moonraker-obico.cfg > "$config_file.tmp"
                mv "$config_file.tmp" "$config_file"

                if [ -n "$token" ]; then
                  echo "Rendered config and preserved auth_token"
                else
                  echo "Rendered config without auth_token; run link flow"
                fi
            securityContext:
              runAsNonRoot: true
              runAsUser: 1000
              runAsGroup: 1000
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
        containers:
          app:
            image:
              repository: ghcr.io/thespaghettidetective/moonraker-obico
              tag: v2.2.3@sha256:4c2e59ccb23799a4f3af7d2f738ccfc5d001bce50d048e57b50f87214d43863f
            command:
              - /opt/venv/bin/python
            args:
              - -m
              - moonraker_obico.app
              - -c
              - /opt/printer_data/config/moonraker-obico.cfg
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
            resources:
              requests:
                cpu: 25m
                memory: 128Mi
              limits:
                memory: 512Mi
          moonraker-proxy:
            image:
              repository: nginx
              tag: 1.31.5-alpine@sha256:72ba65eb42c10344912a84ff42408db7d34f2feb642204570ab8fc5ffd29f1d3
            command:
              - nginx
            args:
              - -c
              - /config/nginx.conf
              - -g
              - daemon off;
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
            resources:
              requests:
                cpu: 5m
                memory: 16Mi
              limits:
                memory: 64Mi

    persistence:
      config:
        storageClass: ceph-block
        accessMode: ReadWriteOnce
        size: 1Gi
        advancedMounts:
          moonraker-obico:
            render-config:
              - path: /opt/printer_data/config
            app:
              - path: /opt/printer_data/config
      logs:
        type: emptyDir
        advancedMounts:
          moonraker-obico:
            app:
              - path: /opt/printer_data/logs
      janus-runtime:
        type: emptyDir
        advancedMounts:
          moonraker-obico:
            app:
              - path: /opt/moonraker-obico/moonraker_obico/bin/janus/runtime
      tmp:
        type: emptyDir
        advancedMounts:
          moonraker-obico:
            app:
              - path: /tmp
            moonraker-proxy:
              - path: /tmp
      config-template:
        type: configMap
        name: moonraker-obico-config
        advancedMounts:
          moonraker-obico:
            render-config:
              - path: /config-template/moonraker-obico.cfg
                subPath: moonraker-obico.cfg
            moonraker-proxy:
              - path: /config/nginx.conf
                subPath: nginx.conf
                readOnly: true
