---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/cilium.io/ciliumnetworkpolicy_v2.json
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: changedetection-ingress-allowlist
spec:
  # Promoted from the kguardian AuditNetworkPolicy baseline (2026-09-16 to
  # 2026-09-18, zero WouldDeny verdicts). Ingress only; egress stays open.
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: changedetection
  ingress:
    # kubelet probes on :5000.
    - fromEntities:
        - host
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: network
            gateway.networking.k8s.io/gateway-name: envoy-internal
      toPorts:
        - ports:
            - port: "5000"
              protocol: TCP
    # Rev-NAT'd DNS replies hit ingress; no toPorts, dest port is
    # ephemeral not 53.
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
