kind: PersistentVolumeClaim

apiVersion: v1

metadata:
  name: example-openebs-pvc
  namespace: default

spec:
  storageClassName: openebs-zfs

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 4Gi
