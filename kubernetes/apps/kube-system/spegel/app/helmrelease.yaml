---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cilium
spec:
  interval: 30m
  chart:
    spec:
<<<<<<<< HEAD:kubernetes/apps/kube-system/spegel/app/helmrelease.yaml
      chart: spegel
      version: v0.0.30
========
      chart: cilium
      version: 1.16.6
>>>>>>>> upstream/main:templates/config/kubernetes/apps/kube-system/cilium/app/helmrelease.yaml.j2
      sourceRef:
        kind: HelmRepository
        name: cilium
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  valuesFrom:
    - kind: ConfigMap
      name: cilium-helm-values
