---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: external-service-httproute-ntopg
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network

  hostnames:
    - ntopg.${SECRET_DOMAIN}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: internal-ntopg
          port: 80
