---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: ipmi-backend-policy
spec:
  targetRefs:
    - kind: HTTPRoute
      name: external-service-httproute-ipmi
  tls:
    validation:
      skip: true
