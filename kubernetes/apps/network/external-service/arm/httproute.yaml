---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: external-service-httproute-arm
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network

  hostnames:
    - arm.${SECRET_DOMAIN}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: internal-arm
          port: 80
