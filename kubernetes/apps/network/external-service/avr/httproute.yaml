---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: external-service-httproute-avr
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network

  hostnames:
    - avr.${SECRET_DOMAIN}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: internal-avr
          port: 80
