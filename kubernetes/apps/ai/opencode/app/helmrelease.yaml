---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app opencode
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template

  values:
    controllers:
      app:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/anomalyco/opencode
              tag: 1.17.14@sha256:1ec1b46bac15a1d99a8f95edc70ad2db6a095109cd6a23d04c55eaf52bd5bd0a
            env:
              TZ: ${TIMEZONE}
              HOME: /home/opencode
              OPENCODE_CONFIG: /config/opencode.jsonc
              LITELLM_API_KEY: ${LITELLM_API_KEY}
            command:
              - opencode
              - web
              - --hostname
              - "0.0.0.0"
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet: &probe
                    path: /
                    port: &port 4096
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  timeoutSeconds: 10
                  failureThreshold: 5
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet: *probe
                  initialDelaySeconds: 15
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 6
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                cpu: 1
                memory: 2Gi

    defaultPodOptions:
      automountServiceAccountToken: true

    service:
      app:
        controller: app
        ports:
          http:
            port: *port

    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: OpenCode
          gethomepage.dev/description: Agentic coding assistant
          gethomepage.dev/group: Agents
          gethomepage.dev/icon: opencode.png
        hostnames:
          - "opencode.${SECRET_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network

    persistence:
      home:
        existingClaim: *app
        globalMounts:
          - path: /home/opencode
      config-file:
        type: configMap
        name: opencode-config
        globalMounts:
          - path: /config/opencode.jsonc
            subPath: opencode.jsonc
