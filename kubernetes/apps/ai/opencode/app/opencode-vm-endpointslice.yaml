---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: opencode-vm
  labels:
    kubernetes.io/service-name: opencode-vm
addressType: IPv4
endpoints:
  - addresses:
      - ${OPENCODE_VM_IP}
ports:
  - port: 4096
    name: http
    protocol: TCP
