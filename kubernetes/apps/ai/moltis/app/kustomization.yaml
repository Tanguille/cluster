---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai

resources:
  - ./helmrelease.yaml

configMapGenerator:
  - name: moltis-config
    files:
      - moltis.toml=resources/moltis.toml
generatorOptions:
  disableNameSuffixHash: true
