---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-server
  namespace: ai
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: ceph-block
  resources:
    requests:
      storage: 20Gi
