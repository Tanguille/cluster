---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ocirepository.yaml
  - helmrelease.yaml
  - grafanadashboard.yaml
  - qwen36-27b-grafanadashboard.yaml
  - qwen36-27b-prometheusrule.yaml

configMapGenerator:
  - name: llmkube-inference-dashboard
    files:
      - dashboard/llmkube-inference.json
    options:
      annotations:
        kustomize.toolkit.fluxcd.io/substitute: disabled
  - name: sglang-dashboard
    files:
      - dashboard/sglang.json
    options:
      annotations:
        kustomize.toolkit.fluxcd.io/substitute: disabled
generatorOptions:
  disableNameSuffixHash: true
