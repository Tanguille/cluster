apiVersion: v1
kind: Secret
metadata:
  name: truenas-mcp
stringData:
  # Placeholder — replace with a minimal read-only TrueNAS API key
  # (TrueNAS UI: System → Operators → API Keys → Add → no permissions).
  TRUENAS_API_KEY: PLACEHOLDER_ADD_READ_ONLY_API_KEY
