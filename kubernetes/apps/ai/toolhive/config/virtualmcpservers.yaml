---
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: VirtualMCPServer
metadata:
  name: flux
  namespace: ai
spec:
  replicas: 2
  sessionStorage:
    provider: redis
    address: dragonfly.database.svc.cluster.local:6379
    db: 0
    keyPrefix: "thv:vmcp:flux:session:"
  incomingAuth:
    type: anonymous
  serviceType: ClusterIP
  groupRef:
    name: flux
  config:
    aggregation:
      conflictResolution: prefix
      conflictResolutionConfig:
        prefixFormat: "{workload}_"
---
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: VirtualMCPServer
metadata:
  name: observability
  namespace: ai
spec:
  replicas: 2
  sessionStorage:
    provider: redis
    address: dragonfly.database.svc.cluster.local:6379
    db: 0
    keyPrefix: "thv:vmcp:observability:session:"
  incomingAuth:
    type: anonymous
  serviceType: ClusterIP
  groupRef:
    name: observability
  config:
    aggregation:
      conflictResolution: prefix
      conflictResolutionConfig:
        prefixFormat: "{workload}_"
---
# Named "ha" to avoid Deployment name collision with MCPServer "homeassistant" (operator creates Deployment = name)
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: VirtualMCPServer
metadata:
  name: ha
  namespace: ai
spec:
  replicas: 2
  sessionStorage:
    provider: redis
    address: dragonfly.database.svc.cluster.local:6379
    db: 0
    keyPrefix: "thv:vmcp:ha:session:"
  incomingAuth:
    type: anonymous
  serviceType: ClusterIP
  groupRef:
    name: homeassistant
  config:
    aggregation:
      conflictResolution: prefix
      conflictResolutionConfig:
        prefixFormat: "{workload}_"
---
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: VirtualMCPServer
metadata:
  name: search
  namespace: ai
spec:
  replicas: 2
  sessionStorage:
    provider: redis
    address: dragonfly.database.svc.cluster.local:6379
    db: 0
    keyPrefix: "thv:vmcp:search:session:"
  incomingAuth:
    type: anonymous
  serviceType: ClusterIP
  groupRef:
    name: search
  config:
    aggregation:
      conflictResolution: prefix
      conflictResolutionConfig:
        prefixFormat: "{workload}_"
---
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: VirtualMCPServer
metadata:
  name: database
  namespace: ai
spec:
  replicas: 2
  sessionStorage:
    provider: redis
    address: dragonfly.database.svc.cluster.local:6379
    db: 0
    keyPrefix: "thv:vmcp:database:session:"
  incomingAuth:
    type: anonymous
  serviceType: ClusterIP
  groupRef:
    name: database
  config:
    aggregation:
      conflictResolution: prefix
      conflictResolutionConfig:
        prefixFormat: "{workload}_"
---
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: VirtualMCPServer
metadata:
  name: resources
  namespace: ai
spec:
  replicas: 2
  sessionStorage:
    provider: redis
    address: dragonfly.database.svc.cluster.local:6379
    db: 0
    keyPrefix: "thv:vmcp:resources:session:"
  incomingAuth:
    type: anonymous
  serviceType: ClusterIP
  groupRef:
    name: resources
  config:
    aggregation:
      conflictResolution: prefix
      conflictResolutionConfig:
        prefixFormat: "{workload}_"
---
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: VirtualMCPServer
metadata:
  name: unified
  namespace: ai
spec:
  replicas: 2
  sessionStorage:
    provider: redis
    address: dragonfly.database.svc.cluster.local:6379
    db: 0
    keyPrefix: "thv:vmcp:unified:session:"
  incomingAuth:
    type: anonymous
  serviceType: ClusterIP
  embeddingServerRef:
    name: toolhive-embeddings
  groupRef:
    name: all
  config:
    aggregation:
      conflictResolution: prefix
      conflictResolutionConfig:
        prefixFormat: "{workload}_"
