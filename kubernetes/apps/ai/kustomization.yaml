---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
components:
  - ../../components/common
resources:
  - ./hermes/ks.yaml
  - ./litellm/ks.yaml
  # Disabled while sglang serves the GPU (single R9700, shared dri) — re-enable to switch back.
  # - ./llama-server/ks.yaml
  - ./open-webui/ks.yaml
  - ./opencode/ks.yaml
  - ./sglang/ks.yaml
  - ./toolhive/ks.yaml
  # Disabled while sglang serves the GPU (single R9700, shared dri) — re-enable to switch back.
  # - ./vllm/ks.yaml
