---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
components:
  - ../../components/common
resources:
  - ./hermes/ks.yaml
  - ./litellm/ks.yaml
  - ./llama-server/ks.yaml
  - ./open-webui/ks.yaml
  - ./opencode/ks.yaml
  # Enable after the first image build + making the GHCR package pullable (plan Step 4):
  # - ./sglang/ks.yaml
  - ./toolhive/ks.yaml
  - ./vllm/ks.yaml
