---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
components:
  - ../../components/common
resources:
  - ./hermes/ks.yaml
  - ./litellm/ks.yaml
  # Operator-managed litellm (Postgres/DB mode), parallel to the file-mode litellm above for
  # validation; swap onto the litellm Service once verified, then remove ./litellm.
  - ./litellm-next/ks.yaml
  - ./memini/ks.yaml
  - ./llmkube/ks.yaml
  - ./opencode/ks.yaml
  - ./odysseus/ks.yaml
  - ./toolhive/ks.yaml
  - ./sglang/ks.yaml
