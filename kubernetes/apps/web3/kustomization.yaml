---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: web3

components:
  - ../../components/common
  # xmrig adds IPC_LOCK, which is outside the baseline capability allowlist.
  - ../../components/psa-privileged

resources:
  - ./monero/ks.yaml
  - ./xmrig-guard/ks.yaml
