---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster.yaml
  - objectstore.yaml
  - pooler.yaml
  - pooler-ro.yaml
  - pooler-session.yaml
  - prometheusrule.yaml
  - scheduledbackup.yaml
  # Basic-auth Secrets backing spec.managed.roles in cluster.yaml (one per adopted app).
  - roles/jellystat.sops.yaml
