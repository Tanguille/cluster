<<<<<<<< HEAD:kubernetes/apps/flux-system/webhooks/app/github/ingress.yaml
========
#% if cloudflare.enabled %#
>>>>>>>> upstream/main:templates/config/kubernetes/apps/flux-system/flux-operator/instance/github/webhooks/ingress.yaml.j2
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flux-webhook
  annotations:
    external-dns.alpha.kubernetes.io/target: "external.${SECRET_DOMAIN}"
spec:
  ingressClassName: external
  rules:
    - host: "flux-webhook.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /hook/
            pathType: Prefix
            backend:
              service:
                name: webhook-receiver
                port:
                  number: 80
