# Handoff: Qwen 3.8 RDNA4 qualification

## Objective
- Qualify Qwen 3.8 27B on the R9700/RDNA4, prefer vLLM if it meets the user's no-more-than-10%-regression performance gate, then create a draft PR with canonical production endpoints.

## Verified facts
- Candidate checkpoint: `cyankiwi/Qwen3.8-27B-AWQ-INT4@63768c10df38c0395e12ef49edac1bd539eaeeea`; it is compressed-tensors W4A16 and stages five safetensor shards — evidence: benchmark manifest and librarian research.
- Production baseline before suspension: C1 `14.96` tok/s, C8 aggregate `36.24` tok/s, C16 aggregate `34.49` tok/s; 13.8K prefill `8.58` tok/s — evidence: `python3 docs/llm-hosting/bench/{concsweep,spectest}.py 30000` against a port-forward to `svc/qwen36-27b`.
- `llmkube-models` is currently suspended and production `qwen36-27b` is stopped — evidence: `kubectl -n ai get kustomization llmkube-models ...` and `kubectl -n ai get inferenceservice ...` on 2026-08-16.
- Temporary vLLM service was patched to one replica; at inspection it was `Creating` with pod `qwen38-27b-bench-vllm-8c46d79d9-jdns5`, `0/1 Running`, four restarts — evidence: same command above.

## Progress
- Done: isolated benchmark manifests, code review/remediation, baseline capture, written live rollback runbook, Flux suspension, production drain, and direct temporary CR apply.
- Remaining: inspect the vLLM pod logs/events; either complete benchmark or restore production immediately; remove temporary resources; reconcile Flux; then implement final cutover only if the performance/correctness gates pass.

## Decisions
- Benchmark only one heavy GPU service at a time. The DRI device plugin exposes multiple scheduling slots for one physical GPU.
- vLLM is the performance candidate. SGLang is boot-compatibility-only because its existing RDNA4 fork has no proven compressed-tensors Qwen 3.8 path.
- DFlash is not enabled: it requires a separate compatible drafter checkpoint and has no gfx1201 validation.
- Production endpoint IDs must be updated to the canonical official Qwen 3.8 name only after qualification.

## Failed attempts
- The 45-minute `kubectl wait --for=condition=Available inferenceservice/qwen38-27b-bench-vllm` was aborted by the user after the replica patch. It produced no readiness result. The current pod had restarted four times; cause is not established.
- `kubectl -n ai get pvc qwen36-27b-model-cache,qwen36-27b-triton-cache,qwen36-27b-hicache` was invalid comma-separated resource syntax; later individual/list output confirmed production PVCs remain bound.

## Pause point
- Live state is unsafe to leave indefinitely: Flux is suspended, production Qwen is stopped, and vLLM is crash-looping/creating. First run:
  `kubectl -n ai get pod qwen38-27b-bench-vllm-8c46d79d9-jdns5 -o yaml; kubectl -n ai logs qwen38-27b-bench-vllm-8c46d79d9-jdns5 --previous; kubectl -n ai get events --sort-by=.lastTimestamp`
- If the cause cannot be resolved promptly, execute the mandatory restore section in `~/.local/state/live-pr-test/ai-qwen38-rdna4-benchmark.md` (explicitly delete the five `qwen38-27b-bench-*` PVCs because they do not carry the documented cleanup label).
- The branch contains the benchmark manifest, kustomization entry, and this handoff; local `.ignore` remains untracked and must not be committed.

## Blockers
- Live vLLM has not reached readiness; root cause, logs, and recovery are required before performance testing.

## Explicit deferrals
- The user authorized live patches in `ai` and requested autonomous continuation to a draft PR.
- Do not run DFlash unless a separate compatible drafter checkpoint is identified and independently qualified.

## References
- Files: `kubernetes/apps/ai/llmkube/models/qwen38-27b-benchmark.yaml`, `kubernetes/apps/ai/llmkube/models/{qwen36-27b-sglang,qwen36-27b-vllm}.yaml`, `docs/llm-hosting/vllm-vs-sglang-2026-07.md`, `.slim/deepwork/qwen-3.8-rdna4.md`.
- Rollback runbook: `~/.local/state/live-pr-test/ai-qwen38-rdna4-benchmark.md`.
- Commands: `kubectl -n ai get kustomization llmkube-models`, `kubectl -n ai get inferenceservice qwen36-27b qwen38-27b-bench-sglang qwen38-27b-bench-vllm -o wide`.

## Validation evidence
- `kustomize build`, YAML semantic checks, `git diff --check`, and `bash .agents/skills/pr-review/scripts/validate-pr.sh` — passed before live apply, reported by implementation specialist on 2026-08-16.
- Oracle manifest review — remediation accepted; required Flux suspension for the production scale-down.

## Session state
- Date: `2026-08-16`
- Branch: `feat/qwen-3.8-27b`
- HEAD: `e7cb04abab84219ec7872a80f1e161268039b55f`
- Worktree status at handoff creation: benchmark changes staged; local `.ignore` untracked.
